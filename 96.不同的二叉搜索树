/*给定一个整数 n，求以 1 ... n 为节点组成的二叉搜索树有多少种？

动态规划：对于1-n结点，那么其内的所有组合是以其中第i个节点为根节点，1-（i-1）节点作为左子树，（i+1）- n作为右子树，
那么将左子树组合数*右子树组合数就是以第i为根节点的组合数量*/

int numTrees(int n) {
  vector<int>dp(n + 1, 0);
	dp[0] = 1;
	dp[1] = 1;
	int i, j;
	for (i = 2; i <= n; i++)
	{
		for (j = 1; j <= i; j++)
		{
			dp[i] += (dp[j-1] * dp[i - j]);
		}
	}
	return dp[n];
  }
